<form method="post" action="{{ path('app_passage_certification_delete', {'id': passage_certification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ passage_certification.id) }}">
    <button class="btn">Delete</button>
</form>
