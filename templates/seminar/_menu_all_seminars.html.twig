<ul class="nav">
    <li class="nav-item">
        <a href="/" class="nav-link">Accueil</a>
    </li>
    {% for seminar in seminars %}
        <li class="nav-item">
            <a href="{{path('app_seminar_read', {'id': seminar.id, 'chapterId':0})}}" class="nav-link">{{seminar.title}}</a>
        </li>
    {% endfor %}
</ul>