{% extends 'base.html.twig' %}
{% block body %}
    <h1 class="text-danger">Une erreur est survenue</h1>
    <p class="p-3 rounded bg-primary-subtle">Pour régler le souci au plus vite,
        contactez votre développeur préféré et fournissez-lui les infos ci-dessous.</p>
    <p class="mt-3 mb-3 p-3 rounded text-light bg-danger fw-bold" style="width: fit-content"">{{error}}</p>
    <div class="p-4 bg-light rounded">
        <h2 class="fs-4">Liste des certifications sélectionnées</h2>
        <ul class="list-group">
            {% for passage in passages %}
                {% set stagiaire = passage.getStagiaire() %}
                {% set certif = passage.getCertification() %}
                <li class="list-group-item">
                    {{"#{stagiaire.prenom} #{stagiaire.nomNaissance}, certification #{certif.getName()} (#{certif.getCode()}) passée le #{passage.dateDebutValidite|date('d-m-Y')}"}}
                </li>
            {% endfor %}
        </ul>
    </div>
    <div class="accordion mt-3" id="accordionXml">
        <div class="accordion-item">
        <h2 class="accordion-header">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
            Afficher le XML
            </button>
        </h2>
        <div id="collapseOne" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
            <div class="accordion-body">
                <pre>{{ result }}</pre>
            </div>
        </div>
    </div>
    <p class="mt-3"><a href="/xml_export">Revenir au formulaire</a>
{% endblock %}